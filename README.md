# 🎬 영화 추천 로직 비교 분석

## 📋 요약

**문제**: 로컬 환경(MovieSir-middle-local)에서는 작동하는 기능이 배포된 서비스(MovieSir-middle)에서는 작동하지 않음

**원인**: 배포 버전에서 AI 모델에 필수 파라미터(`available_time`, `preferred_genres`, `preferred_otts`)를 전달하지 않아 추천 로직이 제대로 작동하지 않음

---

## � 목차

1. [데이터 흐름 비교 (핵심 이해)](#-데이터-흐름-비교-핵심-이해)
   - [배포 버전의 데이터 흐름](#-배포-버전의-데이터-흐름)
   - [로컬 버전의 데이터 흐름](#-로컬-버전의-데이터-흐름)
2. [핵심 차이점 요약표](#-핵심-차이점-요약표)
3. [재추천 기능의 장르 필터링 문제](#-재추천-기능의-장르-필터링-문제)
   - [문제 설명](#문제-설명)
   - [원인 분석](#원인-분석)
   - [해결 방법 (로컬 버전)](#해결-방법-로컬-버전)
   - [배포 버전 vs 로컬 버전](#배포-버전-vs-로컬-버전)
4. [결론](#-결론)

---

## �🔄 데이터 흐름 비교 (핵심 이해)

이 섹션은 사용자가 입력한 시간(예: 300분)이 시스템을 통해 어떻게 흐르는지 명확히 보여줍니다.

### 🔴 배포 버전의 데이터 흐름

```
1. 사용자 입력
   └─ 300분 입력

2. 프론트엔드 (movieApi.ts)
   └─ POST /api/recommend
      {
        runtime_limit: 300,  ← 사용자 입력 시간
        genres: ["액션", "SF"],
        exclude_adult: true
      }
      ❌ available_time 필드 없음

3. 백엔드 Schema (schema.py)
   └─ RecommendationRequest
      - runtime_limit: 300  ✅ 받음
      - genres: ["액션", "SF"]  ✅ 받음
      - exclude_adult: true  ✅ 받음
      ❌ available_time 필드 자체가 없음

4. 백엔드 Service (service.py:20)
   └─ model_instance.predict(user_id, top_k=50)
      ❌ runtime_limit 전달 안 함
      ❌ genres 전달 안 함
      ❌ available_time 전달 안 함

5. AI Model Adapter (ai_model.py:20-28)
   └─ predict() 함수의 기본값 사용
      - available_time: 180 (기본값)
      - preferred_genres: None (기본값)
      - preferred_otts: None (기본값)

6. GPU 서버 호출 (ai_model.py:53-59)
   └─ POST http://10.0.35.62:8001/recommend
      {
        user_movie_ids: [550, 27205, ...],
        available_time: 180,  ← 기본값! (사용자는 300분 입력했지만)
        top_k: 50,
        preferred_genres: null,
        preferred_otts: null
      }

7. GPU 서버 (ai/api.py + v1 알고리즘)
   └─ available_time=180으로 추천 모드 결정
      - 180 < 240 → 단일 영화 추천 모드
      - 180분 기준으로 영화 추천
      - Track A: 장르 필터링 없음 (preferred_genres=null)
      - Track B: 장르 필터링 없음

8. 백엔드로 결과 반환
   └─ [영화ID 리스트] (180분 기준으로 추천된 영화들)

9. 백엔드 필터링 (service.py:38-46)
   └─ 받은 영화들을 runtime_limit=300으로 필터링
      - runtime > 300인 영화 제거
      - genres 필터링 (중복!)
      - 성인 콘텐츠 필터링

10. 최종 결과
    └─ 180분 기준으로 추천된 영화 중
       300분 이하 + 장르 일치하는 영화만 반환
```

> [!CAUTION] > **배포 버전의 문제점:**
>
> - 사용자는 300분을 입력했지만, AI는 180분으로 판단
> - AI가 180분에 최적화된 영화를 추천
> - 백엔드가 이를 300분 기준으로 필터링 (비효율적)
> - 장르 정보가 AI에 전달되지 않아 Track A가 제대로 작동 안 함
> - 백엔드에서 중복 필터링으로 Track B의 다양성 상실

---

### 🟢 로컬 버전의 데이터 흐름

```
1. 사용자 입력
   └─ 300분 입력

2. 프론트엔드 (movieApi.ts)
   └─ POST /api/recommend
      {
        available_time: 300,  ← 사용자 입력 시간 (AI용)
        runtime_limit: 300,   ← 사용자 입력 시간 (필터링용)
        genres: ["액션", "SF"],
        exclude_adult: true
      }
      ✅ available_time 필드 추가

3. 백엔드 Schema (schema.py)
   └─ RecommendationRequest
      - available_time: 300  ✅ 받음
      - runtime_limit: 300  ✅ 받음
      - genres: ["액션", "SF"]  ✅ 받음
      - exclude_adult: true  ✅ 받음

4. 백엔드 Service (service.py:20-26)
   └─ model_instance.predict(
        user_id,
        top_k=50,
        available_time=300,  ✅ 전달
        preferred_genres=["액션", "SF"],  ✅ 전달
        preferred_otts=None
      )

5. AI Model Adapter (ai_model.py:20-28)
   └─ predict() 함수에 파라미터 전달됨
      - available_time: 300  ✅
      - preferred_genres: ["액션", "SF"]  ✅
      - preferred_otts: None

6. GPU 서버 호출 (ai_model.py:53-59)
   └─ POST http://10.0.35.62:8001/recommend
      {
        user_movie_ids: [550, 27205, ...],
        available_time: 300,  ← 사용자 입력값!
        top_k: 50,
        preferred_genres: ["액션", "SF"],  ← 사용자 선택!
        preferred_otts: null
      }

7. GPU 서버 (ai/api.py + v2 알고리즘)
   └─ available_time=300으로 추천 모드 결정
      - 300 < 420 → 단일 영화 추천 모드
      - 300분 기준으로 영화 추천
      - Track A: 장르=["액션", "SF"] + OTT 필터링
      - Track B: 장르 무시 + OTT 무시 (다양성)

8. 백엔드로 결과 반환
   └─ [영화ID 리스트] (300분 기준으로 최적화된 영화들)

9. 백엔드 필터링 (service.py:57-66)
   └─ 최소한의 필터링만 수행
      - 성인 콘텐츠만 필터링
      ✅ 런타임 필터링 제거 (AI가 이미 처리)
      ✅ 장르 필터링 제거 (AI가 이미 처리)

10. 최종 결과
    └─ 300분 기준으로 최적화된 정확한 추천
```

> [!TIP] > **로컬 버전의 개선점:**
>
> - 사용자 입력(300분)이 AI까지 정확히 전달됨
> - AI가 300분에 최적화된 영화를 추천
> - 장르 정보가 AI에 전달되어 Track A가 정확히 작동
> - 백엔드는 성인 콘텐츠만 필터링 (중복 제거)
> - Track B의 다양성 유지 (장르/OTT 무시)

---

## 📊 핵심 차이점 요약표

| 단계                 | 배포 버전             | 로컬 버전                          | 영향      |
| -------------------- | --------------------- | ---------------------------------- | --------- |
| **프론트엔드 전송**  | `runtime_limit`만     | `available_time` + `runtime_limit` | 🔴 치명적 |
| **백엔드 Schema**    | `available_time` 없음 | `available_time` 있음              | 🔴 치명적 |
| **백엔드→AI 전달**   | 파라미터 전달 안 함   | 모든 파라미터 전달                 | 🔴 치명적 |
| **AI 판단 기준**     | 180분 (기본값)        | 300분 (사용자 입력)                | 🔴 치명적 |
| **추천 모드**        | 단일 (180<240)        | 단일 (300<420)                     | 🟡 중요   |
| **Track A 장르**     | 작동 안 함            | 정확히 작동                        | 🔴 치명적 |
| **백엔드 필터링**    | 중복 필터링           | 최소 필터링                        | 🟡 중요   |
| **Track B 다양성**   | 상실                  | 유지                               | 🟡 중요   |
| **재추천 장르 필터** | 필터링 없음           | 장르 필터링 적용                   | 🔴 치명적 |

---

## 🔄 재추천 기능의 장르 필터링 문제

### 문제 설명

사용자가 "재추천 받기" 버튼을 클릭했을 때, 선택한 장르와 다른 영화가 추천되는 문제가 발생합니다.

**예시:**

- 사용자 선택: 1시간, 로맨스 장르
- 초기 추천: 로맨스 영화 3개 표시 ✅
- "재추천 받기" 클릭 → **애니메이션, 판타지 등 다른 장르 영화 표시** ❌

### 원인 분석

#### 1. 백엔드 응답 구조

GPU 서버는 Track A (장르 일치)와 Track B (다양성, 장르 무시) 영화를 구분하여 반환하지만, 백엔드는 이를 하나의 리스트로 합쳐서 반환합니다.

```python
# backend/domains/recommendation/service.py
def get_hybrid_recommendations(...):
    # Track A + Track B 영화를 하나의 리스트로 반환
    return results  # [Track A 영화들 + Track B 영화들]
```

#### 2. 프론트엔드 처리

프론트엔드는 받은 전체 영화 목록을 절반으로 나누어 사용합니다.

```typescript
// frontend/src/api/movieApi.ts
const halfLength = Math.ceil(allMovies.length / 2);
return {
  algorithmic: allMovies.slice(0, halfLength), // Track A + Track B 일부
  popular: allMovies.slice(halfLength), // Track B 일부
};
```

#### 3. 재추천 로직 (배포 버전)

```typescript
// frontend/src/store/useMovieStore.ts (배포 버전)
removeRecommendedMovie: (movieId) => {
  // 전체 목록에서 아직 표시되지 않은 영화 찾기
  const availableMovies = state.allRecommendedMovies.filter(
    (m) => !shownIds.includes(m.id)
  );
  // ❌ 장르 필터링 없음!
  const nextMovie = availableMovies[0];
};
```

**문제점:**

- `allRecommendedMovies`에는 Track A (로맨스) + Track B (다양한 장르) 영화가 섞여 있음
- 재추천 시 장르 필터링을 하지 않아 Track B 영화(로맨스가 아닌 영화)가 선택될 수 있음

### 해결 방법 (로컬 버전)

재추천 시 사용자가 선택한 장르와 일치하는 영화만 선택하도록 필터링을 추가했습니다.

```typescript
// frontend/src/store/useMovieStore.ts (로컬 버전)
removeRecommendedMovie: (movieId) => {
  // 전체 목록에서 아직 표시되지 않은 영화 찾기
  let availableMovies = state.allRecommendedMovies.filter(
    (m) => !shownIds.includes(m.id)
  );

  // ✅ 사용자가 장르를 선택한 경우, 해당 장르와 일치하는 영화만 필터링
  if (state.filters.genres.length > 0) {
    availableMovies = availableMovies.filter(
      (m) => m.genres && m.genres.some((g) => state.filters.genres.includes(g))
    );
  }

  const nextMovie = availableMovies[0];
};
```

**개선 효과:**

- 로맨스를 선택한 경우 → 로맨스 장르가 포함된 영화만 재추천
- 장르를 선택하지 않은 경우 → 모든 영화 허용
- 재추천 가능한 영화가 없으면 → 빈 슬롯으로 표시 (강제로 채우지 않음)

### 배포 버전 vs 로컬 버전

| 항목                   | 배포 버전              | 로컬 버전      |
| ---------------------- | ---------------------- | -------------- |
| **재추천 장르 필터링** | ❌ 없음                | ✅ 있음        |
| **Track B 영화 노출**  | ⚠️ 재추천 시 노출 가능 | ✅ 차단됨      |
| **사용자 경험**        | ❌ 혼란스러움          | ✅ 일관성 있음 |

---

## 📝 결론

배포된 서비스(MovieSir-middle)는 **프론트엔드에서 `available_time`을 전송하지 않고, 백엔드에서 AI 모델에 필수 파라미터를 전달하지 않으며, 백엔드에서 중복 필터링을 수행하고, 구버전(v1) 알고리즘을 사용하며, 재추천 시 장르 필터링을 하지 않아** 추천 로직이 제대로 작동하지 않습니다.

로컬 버전(MovieSir-middle-local)의 변경사항을 배포 버전에 적용하면 문제가 해결됩니다.

> [!TIP]
> 배포 전에 로컬 버전의 파일들을 배포 버전에 복사하는 것이 가장 빠른 해결 방법입니다:
>
> **필수 파일:**
>
> - `frontend/src/api/movieApi.ts` (프론트엔드 API 수정)
> - `frontend/src/store/useMovieStore.ts` (재추천 장르 필터링 추가)
> - `backend/domains/recommendation/schema.py` (스키마 수정)
> - `backend/domains/recommendation/service.py` (서비스 로직 수정)
> - `ai/inference/db_conn_movie_reco_v2.py` (v2 알고리즘)
>
> **선택 파일:**
>
> - `backend/domains/recommendation/ai_model.py` (로깅 개선)
